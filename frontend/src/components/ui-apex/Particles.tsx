// Particles component removed -- cinematic effects are not part of the professional light design system.
// Kept as empty export to prevent import errors if referenced elsewhere.
export function Particles({ count = 0 }: { count?: number }) {
  return null;
}
